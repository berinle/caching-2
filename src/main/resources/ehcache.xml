<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="http://ehcache.org/ehcache.xsd"
         updateCheck="true"
         monitoring="autodetect"
         dynamicConfig="true">

    <diskStore path="java.io.tmpdir"/>

    <defaultCache maxEntriesLocalHeap="1000" eternal="false" timeToIdleSeconds="120" timeToLiveSeconds="120" overflowToDisk="true" statistics="true"/>

    <!-- cache that will hold fiddle instances -->
    <cache name="domain.Fiddle" maxElementsInMemory="20000" statistics="true" eternal="false" timeToIdleSeconds="300" timeToLiveSeconds="600" overflowToDisk="false"/>

    <!-- cache that expires elements after 3secs -->
    <cache name="timedCache" maxEntriesLocalHeap="100" statistics="true" eternal="false" timeToIdleSeconds="2" timeToLiveSeconds="3" overflowToDisk="false"/>

</ehcache>
